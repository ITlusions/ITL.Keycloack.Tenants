# apiVersion: v1
# kind: Secret
# metadata:
#     name: {{ .Release.Name }}-{{ .Values.keycloak.name }}-credentials
# type: Opaque
# data:
# {{- include "itl.keycloack.tenants.secretCredentials" . | indent 4 }}
